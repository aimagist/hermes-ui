{
  "schema_version": 1,
  "source_commit": "631d0f195d70920ae2c205613d582d7ea73078dd",
  "tests_total_at_baseline": 1198,
  "allowed_failed_tests": [
    "AttachmentList renders empty list without error",
    "MessagingView setup-guide link hides the setup-guide button for a plugin platform with no docs URL",
    "ModelSettings assigns an auxiliary task to the main model via setModelAssignment",
    "ModelSettings hides the reasoning/speed defaults when the main model reports no capabilities",
    "ModelSettings loads the current main model and lists configured providers only",
    "ModelSettings renders the auxiliary task rows",
    "ModelSettings shows a persistent banner when a loaded aux slot mismatches the main provider",
    "ModelSettings warns when a main switch leaves auxiliary tasks pinned to another provider",
    "ModelSettings writes the profile default speed (service_tier) when the fast switch is toggled",
    "OAuth onboarding clears stale readiness errors after OAuth succeeds and model confirmation is shown",
    "PaneShell composition uses widthOverride from the store when set",
    "SkillsView toolset management renders a switch for each toolset and toggles it off",
    "assistant-ui streaming renderer renders assistant text incrementally before completion",
    "block-level direction chrome blockquotes carry dir=\"auto\" so the border follows the resolved direction",
    "block-level direction chrome inline code carries dir=\"ltr\" so it does not vote in dir=\"auto\" resolution",
    "block-level direction chrome lists carry dir=\"auto\" so markers follow the resolved direction",
    "block-level direction chrome plain prose blocks stay attribute-free (plaintext CSS owns them)",
    "buildToolView browser_navigate title shows failed title when navigate returns success=false",
    "buildToolView browser_navigate title shows opened title on success",
    "clampForDisplay truncates oversized payloads and reports the omitted count",
    "click-to-edit user message opens the edit composer with the incremental runtime",
    "click-to-edit user message opens the edit composer with the stock runtime",
    "connecting overlay vs recovery surface FIX: once the prolonged reconnect raises a recoverable boot error, the recovery overlay takes over",
    "connecting overlay vs recovery surface hard initial-boot failure surfaces the recovery overlay (the working path)",
    "connecting overlay vs recovery surface post-boot socket drops do not re-cover the app with the initial CONNECTING overlay",
    "panes store width overrides width override is in-memory only — not persisted across reloads",
    "requestModelOptions falls back to REST when no gateway is connected",
    "requestModelOptions passes the active session id and refresh flag through the gateway",
    "requestModelOptions uses the connected gateway even before a session exists",
    "usePreviewRouting does not auto-open a preview from tool results",
    "usePromptActions sleep/wake session recovery recovers via session.resume when prompt.submit TIMES OUT and a stored session is selected (#55578)"
  ]
}
